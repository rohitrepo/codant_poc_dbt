# This file lists the dbt packages this project depends on.
# To install or update these packages, run `dbt deps` from your command line.

packages:
  - package: dbt-labs/dbt_utils
    version: 1.1.1 # Or the latest compatible version
    # Check for the latest version on https://hub.getdbt.com/dbt-labs/dbt_utils/latest/

  - package: calogica/dbt_expectations
    version: 0.10.3 # Or the latest compatible version
    # Check for the latest version on https://hub.getdbt.com/calogica/dbt_expectations/latest/